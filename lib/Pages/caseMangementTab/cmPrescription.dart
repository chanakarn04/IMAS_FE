import 'package:flutter/material.dart';

class CMPrescriptionTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Prescription'),
    );
  }
}
