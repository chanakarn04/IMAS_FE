import 'package:flutter/material.dart';

class AssessmentHistoryPage extends StatelessWidget {
  static const routeName = '/assessment history';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('ass hst')),
    );
  }
}
