import 'package:flutter/material.dart';

class DiseaseSymptomTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('Disease/Symptom tab.'),
      ),
    );
  }
}
