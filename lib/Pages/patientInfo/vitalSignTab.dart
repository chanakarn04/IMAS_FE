import 'package:flutter/material.dart';

class VitalSignTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('Vital sign tab.'),
      ),
    );
  }
}
